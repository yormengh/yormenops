{
  "name": "yormenops-lambda",
  "version": "1.0.0",
  "description": "YormenOps — Lambda handlers for the tech & DevOps journal API",
  "main": "src/handlers/index.js",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/.bin/jest",
    "lint": "eslint src --ext .js",
    "package": "zip -r ../lambda.zip . --exclude 'node_modules/*' --exclude '*.test.js'",
    "local": "node src/local.js"
  },
  "dependencies": {
    "mongoose": "^8.2.0",
    "slugify": "^1.6.6"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=20.x"
  }
}
