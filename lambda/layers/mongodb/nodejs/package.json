{
  "name": "yormenops-mongodb-layer",
  "version": "1.0.0",
  "description": "Lambda Layer — mongoose + slugify bundled for YormenOps",
  "dependencies": {
    "mongoose": "^8.2.0",
    "slugify": "^1.6.6"
  }
}
